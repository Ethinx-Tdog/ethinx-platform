repos:
  - repo: local
    hooks:
      - id: ethinx-catalog-validate
        name: ETHINX catalog validator (AUD immutable)
        entry: python catalog/validate_catalog.py
        language: system
        pass_filenames: false
        files: ^catalog/products/.*\.(yml|yaml)$
