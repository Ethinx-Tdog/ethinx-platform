# ethinx-platform

**The core automation engine of ETHINX**

- FastAPI backend
- Redis Streams for real-time processing
- Plugin runtime system
- RAG (Retrieval Augmented Generation) ready
- Full observability

## Quick Start
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
